# Tuple est une collection ordonnée avec doublons autorisés. Il s'agit d'une liste en lecture seule (non modifiable)

# Création d'un tuple vide:
t = ()

prenoms = ("Benjamin", "Julien", "Mathéo")

# pour modifier un tuple -> on le converti en liste:
list = list(prenoms)

